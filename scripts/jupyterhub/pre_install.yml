- file:
    state: "directory"
    path: "{{ data_path }}/jupyterhub"
    owner: 1000
    mode: 0755
    recurse: yes

- file:
    state: "directory"
    path: "{{ item }}"
    owner: 1000
    mode: 0777
  with_items:
    - "{{ tmp_path }}/spark"
    - "{{ tmp_path }}/spark/local"